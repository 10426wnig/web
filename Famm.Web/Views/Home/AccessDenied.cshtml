@{
    ViewBag.Title = "Доступ запрещен";
}

<div class="section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <div class="p-5">
                    <i class="bi bi-shield-lock text-danger" style="font-size: 5rem;"></i>
                    <h1 class="mt-4 mb-4">Доступ запрещен</h1>
                    <p class="lead mb-4">У вас нет необходимых прав для доступа к этой странице. Доступ к админ-панели разрешен только администраторам.</p>
                    <div class="mt-5">
                        <a href="@Url.Action("Index", "Home")" class="btn btn-primary btn-lg">Вернуться на главную</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 